import React from 'react';
import './MetaMaskLocked.css';

const MetaMaskLocked = () => (
  <div className="metamask-locked container">
    <h2>Your MetaMask is locked</h2>
    <p>Simply open MetaMask and follow the instructions to unlock it.</p>
  </div>
);

export default MetaMaskLocked;
